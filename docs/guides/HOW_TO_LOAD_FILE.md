# 📄 Как загрузить файл для проверки - Простая инструкция

## 🎯 Главное правило

**Ваш файл с документацией должен быть на вашем компьютере, в любой папке.**

API не загружает файлы с диска - вы сами читаете файл и отправляете его содержимое.

---

## 📝 Шаг 1: Где положить файл?

Положите ваш файл с документацией **в любую папку** на вашем компьютере.

**Примеры:**
- `C:\Users\ВашеИмя\Documents\my_docs.md`
- `C:\git\Git2\my_documentation.md`
- `C:\Projects\docs\architecture.md`

**Неважно где!** Главное - знать полный путь к файлу.

---

## 📝 Шаг 2: Как указать путь к файлу?

### Вариант А: Файл в той же папке, где ваш скрипт

Если ваш файл `my_docs.md` лежит в той же папке, где вы запускаете Python:

```python
# Просто имя файла
with open("my_docs.md", "r", encoding="utf-8") as f:
    document = f.read()
```

### Вариант Б: Файл в другой папке

Если файл в другой папке, укажите **полный путь**:

```python
# Windows путь
with open("C:\\Users\\ВашеИмя\\Documents\\my_docs.md", "r", encoding="utf-8") as f:
    document = f.read()

# Или относительный путь от текущей папки
with open("docs\\my_docs.md", "r", encoding="utf-8") as f:
    document = f.read()
```

---

## 📝 Шаг 3: Полный пример

```python
import requests

# 1. Укажите путь к ВАШЕМУ файлу
file_path = "my_documentation.md"  # ← ЗАМЕНИТЕ на путь к вашему файлу!

# 2. Прочитайте файл
with open(file_path, "r", encoding="utf-8") as f:
    document = f.read()

# 3. Отправьте на анализ
response = requests.post(
    "http://localhost:8000/api/v1/review/start",
    json={
        "document": document,  # ← Содержимое файла
        "document_type": "markdown",
        "context": {"project_type": "microservices"}
    }
)

# 4. Получите task_id
task_id = response.json()["task_id"]
print(f"Файл загружен! Task ID: {task_id}")
```

---

## 🎯 Конкретные примеры

### Пример 1: Файл на рабочем столе

```python
# Файл: C:\Users\Иван\Desktop\docs.txt
with open("C:\\Users\\Иван\\Desktop\\docs.txt", "r", encoding="utf-8") as f:
    document = f.read()
```

### Пример 2: Файл в папке проекта

```python
# Файл: C:\git\Git2\my_docs.md
with open("my_docs.md", "r", encoding="utf-8") as f:
    document = f.read()
```

### Пример 3: Несколько файлов

```python
# Файлы в папке docs/
files = [
    "docs/architecture.md",
    "docs/api.md",
    "docs/security.md"
]

# Читаем все файлы
documents = []
for file_path in files:
    with open(file_path, "r", encoding="utf-8") as f:
        documents.append(f.read())

# Объединяем в один текст
document = "\n\n".join(documents)
```

---

## ❓ Частые вопросы

### Вопрос: Нужно ли загружать файл на сервер?

**Ответ:** НЕТ! Файл остается на вашем компьютере. Вы просто читаете его содержимое и отправляете текст через API.

### Вопрос: Какой формат файла нужен?

**Ответ:** Любой текстовый формат:
- `.md` (Markdown) - рекомендуется
- `.txt` (обычный текст)
- `.rst` (reStructuredText)
- `.docx` - нужно сначала конвертировать в текст

### Вопрос: Можно ли указать файл в интернете?

**Ответ:** Да, но сначала нужно скачать его:

```python
import requests

# Скачиваем файл из интернета
url = "https://example.com/docs.md"
response = requests.get(url)
document = response.text

# Теперь отправляем на анализ
# ... (как в примере выше)
```

### Вопрос: Что если файл очень большой?

**Ответ:** Система может обработать большие файлы, но рекомендуется:
- До 10,000 строк - оптимально
- 10,000-50,000 строк - может занять больше времени
- Более 50,000 строк - разбейте на части

---

## 🚀 Быстрый старт

1. **Создайте файл** с вашей документацией (например, `my_docs.md`)
2. **Положите его** в любую папку (например, рядом со скриптом)
3. **Используйте код выше**, заменив `"my_docs.md"` на путь к вашему файлу
4. **Запустите скрипт**

**Готово!** 🎉

---

## 📚 Дополнительно

- Полное руководство: [USER_GUIDE.md](USER_GUIDE.md)
- Примеры кода: [examples/load_documentation.py](examples/load_documentation.py)

